<h1>My Tasks</h1>
<div class="container">
  <%= link_to "New Task", new_task_path, class: "btn btn-dark" %>
  <ul>
    <% @tasks.each do |task| %>
      <li>
        <%= link_to task.title, task_path(task) %>
      </li>
    <% end %>
  </ul>
</div>
